<!doctype html>
<html lang="en">
<% include ../partials/header.ejs %>
<title>index</title>
<body>
<% include ../partials/nav.ejs %>
<div class="container-fluid">
  <div class="row">
    <div class="border col-9">
      <h1>content</h1>
      <% buyGroupListData.forEach(function(item){ %>
        <p><%= item.name %></p>
        <p><%= item.description %></p>
      <% }); %>
    </div>
    <div class="border col-3">menu</div>
  </div>
</div>
</body>
</html>